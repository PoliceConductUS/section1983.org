---
import { getCollection } from "astro:content";
import Base from "../layouts/Base.astro";
import Breadcrumbs from "../components/Breadcrumbs.astro";

const articles = await getCollection("articles", ({ data }) => !data.draft);
const cases = await getCollection("cases");
const termsAndConceptsEntries = await getCollection("termsAndConcepts");
const process = await getCollection("process");

const sortedArticles = articles.sort((a, b) => {
  const da = a.data.pubDate ? new Date(a.data.pubDate).getTime() : 0;
  const db = b.data.pubDate ? new Date(b.data.pubDate).getTime() : 0;
  return db - da;
});
const sortedCases = cases.sort((a, b) =>
  a.data.title.localeCompare(b.data.title),
);
const sortedTermsAndConcepts = termsAndConceptsEntries.sort((a, b) =>
  a.data.term.localeCompare(b.data.term),
);
const sortedProcess = process.sort((a, b) => a.data.order - b.data.order);
const canonicalUrl = "https://www.section1983.org/sitemap/";
const structuredData = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  name: "section1983.org Site Map",
  description: "Complete directory of the public pages on section1983.org.",
  url: canonicalUrl,
};
---

<Base
  title="Site Map"
  description="Complete directory of all pages on section1983.org."
  canonicalUrl={canonicalUrl}
  structuredData={structuredData}
>
  <Breadcrumbs crumbs={[{ label: "Home", href: "/" }, { label: "Site Map" }]} />

  <h1 class="text-3xl font-bold text-navy mb-8">Site Map</h1>

  <div class="grid gap-8">
    <section>
      <h2 class="text-xl font-bold text-navy mb-3 border-b-2 border-gold pb-2">
        Pages
      </h2>
      <ul class="space-y-1 text-sm">
        <li>
          <a
            href="/"
            class="focus-ring surface-link text-navy transition-colors">Home</a
          >
        </li>
        <li>
          <a
            href="/about"
            class="focus-ring surface-link text-navy transition-colors">About</a
          >
        </li>
        <li>
          <a
            href="/statute-of-limitations/"
            class="focus-ring surface-link text-navy transition-colors"
            >Statute of Limitations by State</a
          >
        </li>
        <li>
          <a
            href="/downloads/"
            class="focus-ring surface-link text-navy transition-colors"
            >Sample Documents</a
          >
        </li>
        <li>
          <a
            href="/about/section-1983"
            class="focus-ring surface-link text-navy transition-colors"
            >About § 1983</a
          >
        </li>
        <li>
          <a
            href="/get-involved"
            class="focus-ring surface-link text-navy transition-colors"
            >Get Involved</a
          >
        </li>
        <li>
          <a
            href="/search"
            class="focus-ring surface-link text-navy transition-colors"
            >Search</a
          >
        </li>
        <li>
          <a
            href="/legal-notice"
            class="focus-ring surface-link text-navy transition-colors"
            >Legal Notice</a
          >
        </li>
        <li>
          <a
            href="/terms"
            class="focus-ring surface-link text-navy transition-colors"
            >Terms & Conditions</a
          >
        </li>
        <li>
          <a
            href="/accessibility"
            class="focus-ring surface-link text-navy transition-colors"
            >Accessibility</a
          >
        </li>
      </ul>
    </section>

    <section>
      <h2 class="text-xl font-bold text-navy mb-3 border-b-2 border-gold pb-2">
        Articles ({sortedArticles.length})
      </h2>
      <ul class="space-y-1 text-sm">
        {
          sortedArticles.map((a) => (
            <li>
              <a
                href={`/articles/${a.id}/`}
                class="focus-ring surface-link text-navy transition-colors"
              >
                {a.data.title}
              </a>
            </li>
          ))
        }
      </ul>
    </section>

    <section>
      <h2 class="text-xl font-bold text-navy mb-3 border-b-2 border-gold pb-2">
        Case Law Library ({sortedCases.length})
      </h2>
      <ul class="space-y-1 text-sm columns-2">
        {
          sortedCases.map((c) => (
            <li>
              <a
                href={`/cases/${c.id}/`}
                class="focus-ring surface-link text-navy transition-colors"
              >
                {c.data.title}
              </a>
            </li>
          ))
        }
      </ul>
    </section>

    <section>
      <h2 class="text-xl font-bold text-navy mb-3 border-b-2 border-gold pb-2">
        Terms and Concepts ({sortedTermsAndConcepts.length})
      </h2>
      <ul class="space-y-1 text-sm columns-2">
        {
          sortedTermsAndConcepts.map((p) => (
            <li>
              <a
                href={`/terms-and-concepts/${p.id}/`}
                class="focus-ring surface-link text-navy transition-colors"
              >
                {p.data.term}
              </a>
            </li>
          ))
        }
      </ul>
    </section>

    <section>
      <h2 class="text-xl font-bold text-navy mb-3 border-b-2 border-gold pb-2">
        The Legal Process ({sortedProcess.length})
      </h2>
      <ul class="space-y-1 text-sm">
        {
          sortedProcess.map((p) => (
            <li>
              <a
                href={`/process/${p.id}/`}
                class="focus-ring surface-link text-navy transition-colors"
              >
                Step {p.data.order}: {p.data.title}
              </a>
            </li>
          ))
        }
      </ul>
    </section>
  </div>
</Base>
